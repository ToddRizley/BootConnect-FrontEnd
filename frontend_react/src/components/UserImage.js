import React, { Component } from 'react'
import { connect } from 'react-redux'
import { bindActionCreators } from 'redux'

const UserImage = class extends Component {
  render(){
    return(
      <div>
        User image
      </div>
    )
  }
}

export default UserImage
